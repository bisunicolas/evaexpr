// TODO: Ajouter ici une fonction "handleClick" qui prend "className" en argument
// et qui l'utilise pour modifier "document.body.className"
function handleClick(className) {
    document.body.className = className;
}

// function handleClick1(className) {
//     document.getElementById"hi".className = className;
// Je pense à utiliser var choisirmondiv = document.getelementbyID('whatever'); var mondivcolor = choisirmondiv.color; }